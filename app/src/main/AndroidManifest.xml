<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.bootcamp.ivallavifahrazi_mealdb">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/ic_baseline_set_meal_24"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_baseline_set_meal_24"
        android:supportsRtl="true"
        android:theme="@style/Theme.IvallaviFahrazi_mealdb"
        tools:targetApi="31">
        <activity
            android:name=".ui.DetailFavoriteActivity"
            android:exported="false" />
        <activity
            android:name=".ui.FavoriteActivity"
            android:exported="false" />
        <activity
            android:name=".ui.DetailMealsActivity"
            android:exported="false" />
        <activity
            android:name=".ui.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>